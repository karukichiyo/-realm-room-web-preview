# Preview25 — 玩家独立装修流程整理

源目录：`work/realm-room-preview10-20260922/godot`
运行版本：`realm-room-preview25-decoration`

## 玩家使用顺序

1. 装修 → 导入新家具。先选落地家具、地毯、墙面装饰、分层窗户、吊挂灯具或桌面小物，再到图像页上传 PNG。墙纸、地板和背景从装修的专用入口上传 PNG/JPG。
2. 已画透视的素材保持原图；平面纹理才使用自动透视。同一分层窗户的窗框、窗景、玻璃使用相同尺寸画布。
3. 大小页调整图片和占地的整体比例；占地页直接显示接地轮廓。拖动两侧圆点改变占地宽深，前端圆点校准落脚处。精细选项默认折叠。
4. 应用设计后，到摆放家具选择该设计。局部应用、摆放、材质位置应用都留在装修预览中，最后点击确定装修才提交正式布局。
5. 预览条的信息图标打开装修检查和简明指南。保存检查发现新问题时列出家具名称和原因，点该家具可返回调整。已有旧存档的问题允许逐步修复，不强制玩家一次性修好全部旧素材。
6. 放弃装修会询问并还原本次修改。刷新后可恢复已接受的装修草稿；未应用的局部大小编辑不进入草稿。草稿不自动覆盖正式存档。
7. 更多 → 完整房间 · 导入/导出，可下载包含素材的完整 JSON 备份。草稿冲突时允许导出为同样的完整备份格式，避免覆盖已改变的正式房间。

## 实现与故障修正

- main.gd：装修入口四格分组，直接新建入口；家具列表选择保持相机；大小局部应用不结束事务，单独撤销记录；具体问题列表替代笼统无法保存提示；工作台保存失败保持编辑器与原设计。
- asset_editor.gd：用途预设，六个页签，用途与专家分类分离；图像与占地不默认联动；图片更换清除失效的旧裁剪/网格/碰撞分块；估算接地锚点可保留；多块碰撞按宽深缩放；在无边框网页中显示导入错误，不能只修改不可见的窗口标题。
- footprint_canvas.gd：最近控制点命中，扩大触摸范围，鼠标/触摸输入；像素图片上叠加接地轮廓。纯平面材质不显示无效图片锚点手柄。
- decoration_session.gd：独立草稿、提交前检查、基于已持久化数值表示的校验摘要（避免 JSON 整数转浮点使草稿失配）。正式布局与草稿分开保存；不把角色日常变化用于装修恢复判断。
- console_home.gd：恢复提示、检查入口与明确的未应用状态。
- room_data.gd：尊重玩家校准的墙面图片锚点，保留旧默认兼容行为。
- function_ui.gd：网格与窗户几何图标、页签选择 API、统一按键反馈继承。

## 验证

- 新增 test_decoration_journey25：空导入反馈、上传、独立占地调整、地毯语义、摆放、取消还原、局部尺寸应用、撤销重做、正式保存、完整备份解析、越界拒绝覆盖。
- 新增 test_decoration_recovery25：模拟重启，正式房间不自动替换；恢复草稿；未应用尺寸不写入草稿；取消清除。
- 新增 test_contact_input25：触摸拖动和重叠热区选择。
- 回归：room_geometry、material_controls、multi_light_material_modes、projection_registration、compound_occlusion、occlusion_glass24、rug_navigation_ui、editor_camera_layers、furniture_projection_environment。
- 浏览器：450×800，真实文件输入控件上传 PNG、工作台应用、摆放、确认、刷新、下载包含图片的完整备份、重新上传并确认导入。截图与备份位于 work/preview25-release。浏览器为桌面 Edge 的手机尺寸视口，不代表真实 iPhone Safari 性能测试。

## 边界与下一步

任意复杂几何体无法仅从二维图片自动准确推断真实接地范围。已有复合沙发的分块数据保留；当前交互校准是整体边界和锚点，不是任意多边形/逐块接地绘制工具。透明玻璃和外观美术仍依赖上传素材。草稿属于当前浏览器存储，不提供跨设备同步，也不保证浏览器清理或设备断电前每个瞬间都已落盘。当前改进不能宣称零故障。

发布脚本：work/build_preview25.py、work/prepare_local25.py。
测试脚本：work/test_decoration25.py、work/browser_journey25.cjs。
回滚基线：Preview24，GitHub commit fcac25858988f6f5009911a483b766b0ba67b97d。
