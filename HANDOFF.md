# Preview17 · 归档透视素材与分层窗户

2026-09-24，接续 preview16。源码分支为 `codex/preview17-archive-assets`，本次源码提交及 GitHub Actions 生成的构建提交记录在 `release.json`。

用户提供的 23 张 2000×2000 透明 PNG 已按原目录与文件名含义整理成 18 个设计：灰石地板、毛布地毯、组合沙发、深棕圆桌、两帧木柜、大型盆栽、两种装饰柱、两种吊灯、左右墙纸、三块立体墙面、窗帘，以及左右墙各一组分层窗户。全部使用 `isometric_sprite`，只裁去透明边、按网格缩放和排序，不再二次做透视变形；已有左右朝向的素材禁止自动镜像。

左右窗户各由“窗景（最底层）/玻璃（中层，可选）/窗框（最上层）”组成。家具资产编辑器把三个上传项按这个顺序单列显示，三个层均可独立替换。木柜常态与打开帧合并为同一件可交互家具。

素材全部加入“归档素材 / 预绘透视”素材库。房间管理新增“打开归档素材展示房”，会先保存当前房间，再建立 12×12 展示房并摆出 18 个设计；更新网站不会自动覆盖用户当前房间。

绘制层级增加 `visual_depth_bias`，用于墙纸、立体墙、窗户和窗帘的稳定前后关系。墙面与地面都支持该偏移。素材来源统一放在 `godot/assets/archive_pack`，清单与展示房生成逻辑在 `godot/scripts/archive_asset_pack.gd`。

本机回归通过：归档素材清单与图片加载、家具投影与环境、地毯导航、20 个功能页面、桌面壳层。私有 GitHub Actions 又完成一次 4.6.3 正式 Web 导出并提取增量发布文件。公开站仍从 preview9 完整基础包增量合并，因此保留此前全部家具、角色和素材。
