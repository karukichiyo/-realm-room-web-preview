# Preview15 · 电脑版与桌边陪伴

2026-09-22，接续 preview14。源码仍在 `work/realm-room-preview10-20260922/godot`。独立 living-v2 样板没有覆盖原工程。

## 已实现

- 桌面宽屏自动切换为左侧房间、右侧操作页；提供宽屏、侧边、小窗及全屏切换。侧边为上方场景、下方操作；小窗收起编辑导航。手机首次打开维持原版布局。
- Windows 原生小窗支持置顶、顶部空白处拖动、无边框陪伴窗口；Esc 退出原生全屏。网页只能在浏览器内部使用对应布局，不能悬浮于其他应用。
- 奶油色 / 复古蓝、几何图标、统一按压与回弹。复用原有操作页面、装修草稿及保存逻辑，不另造平行存档系统。
- 默认 24 分钟完成八阶段昼夜循环，可暂停；环境色、窗光强度等逐渐过渡。只累计活动时间，不补算挂起时间。装修预览及联机期间不自动改时间。临时混光参数在存档归一化时清除。
- 房间 / 小人两个模式。可从房间内和角色库选择 1–6 位伙伴，有搜索。不会把角色库成员强行搬入房间，也不修改原房间坐标。
- 小人模式复用角色帧动画，支持点击招呼、点击空地移动选中角色、拖动、邀请相处、自主走动和交流；可暂停自主活动。使用现有性格和关系系统，保留社交冷却，不靠连点刷好感。没有接入生成式 AI。
- 小人模式暂停隐藏房间的模拟，避免一份角色同时在两处行动。退出后恢复房间。桌边显示位置属于临时视图，不保存为房间位置。
- 小窗打开播放器等操作时暂时展开，关闭后回小窗；手动选择其他布局可覆盖这个临时返回行为。

## 文件与重建

新增 `desktop_shell.gd`、`companion_stage.gd`，无 class_name，由 preload 引用。修改 console_home/main/function_ui/room_view/subtitle_rail/environment_profile/room_data。

`work/build_desktop_release.py` 从 preview14 的全部编译补丁继续构建，保留原始 preview9 素材、场景、导入纹理字节不变。`web/index.pck` 仅为编译提取中间包，实际发布包见 `site/` 和 `release.json`。使用 Godot 4.6.3 `--export-pack Web`，无需 Web 导出模板。最终 PCK 的散列和大小以 release.json 为准。

Windows 文件夹使用同一个实际发布 PCK，附 Godot 4.6.3 引擎、MIT/第三方版权说明和字体许可；通过“启动桌面版.vbs”以 `-- --desktop` 启动。原生端与浏览器存档独立。当前原生联机地址仍为开发用 localhost:8767；此便携包是本地桌边陪伴版，远程联机继续用网站。没有声称透明桌宠、跨软件穿透或移动后台常驻已实现。

## 验证

- `test_desktop_shell.gd`：宽屏分区、操作页停靠、时间推进/暂停/防离线快进、混光清理、多角色真实关系交流、冷却、不修改房间坐标、角色库保留、单人招呼、窄窗几何、联机保护、装修草稿保护、按压回弹、返回原手机父节点。
- `test_desktop_pages.gd` / `test_function_pages.gd`：桌面 / 手机各 20 个页面、返回栈、分类切换、共同主题和按键响应。测试显式创建示例房间，不再依赖本机已有角色。
- 原有角色库/成长、地毯/寻路/天气、字幕位置、角色帧动画回归。旧 test_workbench 的“关闭装修立即退出”断言与既有装修草稿确认逻辑不符，不把它算作通过。
- GPU 原生实际渲染截图在本目录；没有声称完成真实 iPhone 或真实浏览器自动化。已知根证书读取提示与脚本报错分开检查。

## 后续

优先收集实际电脑宽屏、小窗尺寸与角色密度反馈，再细化面板信息密度。桌边多角色上限暂为六人。原版角色养成总方案及 preview14 的真实经历/习惯校准继续保留；本轮未实现外出地图、分部件换装、动态 AI 台词。
