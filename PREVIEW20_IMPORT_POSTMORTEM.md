# Preview20：透视素材导入故障复盘

## 结论

这次不是绘图错误。原始素材使用同一个 2000×2000 画布、同一套 16×18 网格；导入器却分别猜测大小、锚点和占地，并在 12×12 展示房里重新摆放，破坏了原稿的共同坐标。之前仅验证素材能加载，没有对照原稿验证比例和相对位置，是验证遗漏。

## 原因与修复

| 问题 | 原因 | 修复 |
|---|---|---|
| 家具比例不同、窗户过大 | 每张图使用独立经验缩放，透明边缘与透视斜率被算入墙面高度 | 统一源网格换算；窗户占墙高度扣除横向透视斜率 |
| 墙纸和地板偏移 | 裁切后丢失共同画布原点，再按各自外框映射 | 保留源画布定位参数，按真实墙面/地面四角采样 |
| 调整不能移动或缩小 | 缩放最小值为 1，位置被强制夹回覆盖范围；透视手势坐标不一致 | 墙地允许 0.1–8 倍缩放和自由偏移，采用实际贴图坐标，确认后保存 |
| 立体墙面出现在墙纸列表 | 分类错误 | 三项立体墙面作为墙面家具；墙地页面恢复紧凑入口 |
| 家具占地不一致 | 用整张图片外框代替接地范围 | 使用接地边缘与独立碰撞区域；组合沙发使用分段碰撞，毯子和凹口不阻挡 |
| 吊灯被家具遮住 | 使用包含悬挂高度的图片底部作为排序值 | 按世界位置、实际高度范围及重叠关系排序；保留地毯底层规则 |
| 窗户不随时刻改变 | 窗户仅混入少量环境色 | 窗户各层参与时刻环境色调制 |
| 编辑后再次变形 | 墙面家具被强制改成平面贴图；已标定家具再次自动估算占地 | 编辑器保留已有透视与标定，15 项素材逐一验证打开后保存不改尺寸、锚点与占地 |

## 本次素材的标定

- 原画布：2000×2000；房间：16×18，高 5.5 格。
- 原点：(1054, 756)；X 轴：(55.5, 27.75)；Y 轴：(-55.5, 27.75)；Z 轴：(0, -83.25)。
- 到引擎统一比例：32 / 55.5。所有素材共享这个比例，不再逐个猜测。
- 展示房含 15 项家具/窗户、3 项墙地材质。窗框、窗景、玻璃保留分层。
- 每层裁切范围、源网格、物理范围与原始摆放位置写入 `assets/archive_pack/calibrated-layout.json`。

## 后续导入约定

导出网格 PNG 时同时下载网格定位 JSON。绘图时保留完整画布；允许整体等比例缩放，不要给单个图层单独裁切或加边。导入后使用网格参数校准。比例不一致时明确提示，不再静默猜测。图片不再被静默压到 1024 像素。

图片本身不能可靠表达接地范围、悬挂高度和凹形碰撞。任意新家具仍需要在实时预览里核对占地与高度；复杂组合物应提供碰撞分区。不能把所有透明轮廓自动当成实体。

## 存档兼容

升级内置素材定义与误分类的已知墙面资源，保留用户上传文件。不会用展示房覆盖现有装修。通过“更多 → 打开归档素材展示间”打开校准后的 16×18 原稿展示房；当前房间先存档。用户另存、手动改造过的家具副本不强制覆盖。

## 回归范围

新增共同画布注册测试，逐层比对源图左上角与引擎投影，误差要求小于 0.01 像素；验证墙面边界、自由移动与缩小保存、吊灯高低排序、组合沙发与边桌共存、窗户昼夜变化、旧墙面迁移及紧凑页面。另运行网格、家具、地毯导航、尺寸、素材包与灯光相关测试，并在两个全新浏览器上下文加载展示房检查视觉结果。

浏览器验证不能替代所有真实手机的触摸与文件选择验证；复杂任意形状的遮挡仍需要资产元数据和具体场景回归。
