# Preview19：素材跨浏览器可见与存储准备

## 已实现

- 修复 main.gd 从 room_workspace.json 读取时清空新版默认素材库的问题。统一经 RoomData.merge_library 合并；用户编辑过的同 ID 设计优先，不覆盖个人设计。
- 归档包分成 12 件家具/窗户与 6 项墙地材质；窗户保留窗景、玻璃、窗框分层。
- 旧墙纸/地板家具实例迁移到 room.materials；已有透视材质去透明留白，走 perspective 模式，不重复扭曲。
- 更多中提供归档展示间、素材与本机存储入口。展示间切换前保留原房间存档。
- 墙地导入按 PNG 内容 SHA-256 保存，相同内容复用文件。
- 存储页统计 Godot user:// 文件体积（不是整个浏览器配额）；清理只针对 room_materials 中未被当前数据、JSON 存档/备份、撤销/重做引用的 PNG。
- 完整房间备份兼容归档包的图片总像素；仍有 96 MB 文件、64 MB 媒体、单图 4096px 限制，图片逐个验证。
- 每件家具多光源与平面图/已有透视模式一并上线。

## 预备的素材包协议

assets/archive_pack/catalog.json：schema_version、包 ID、版本、分类、材质记录、文件路径、字节数与 SHA-256。
当前 source=bundled，保持离线/旧存档可用；不能把它描述成已实现远程按需下载。

下一阶段按 furniture/windows/materials/characters/places 分包。清单保留稳定资产 ID；缩略图先载入，确认使用后下载图片。远程地址必须 HTTPS，逐文件校验 SHA-256 后原子写入内容寻址缓存；失败保留旧版本；用户保存的房间引用包 ID+版本+文件哈希。主动清理需检查所有存档与撤销记录。

对象存储由用户选择服务后接入，可更换 URL 而不改变内容哈希。大文件下载量与存储费用属于该服务，不能承诺永久免费。账号同步与浏览器本地存档是不同层，复制链接不会复制个人存档。

## 发布验证

从只含 scripts/scenes/assets 的干净目录导出，避免测试用户数据进入 PCK。使用现有 Pages 基础包，替换编译脚本和新材质导入资源；不改变站点 origin 与存档路径。每次发布用新的 runtime 名称更新 Service Worker 缓存。
测试包括旧版素材库合并、墙地迁移、归档展示间、完整备份往返、多光源与功能页面。通过两个全新浏览器上下文检查自带内容，并测试带旧库的上下文。
